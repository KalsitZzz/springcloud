server:
  port: 80
spring:
  application:
    name: cloud-consumer-order80

eureka:
  client:
    register-with-eureka: true #是否注册到eureka
    fetchRegistry: true #是否从注册中心获取注册服务
    service-url: #指定eureka的地址
      defaultZone: http://localhost:7001/eureka